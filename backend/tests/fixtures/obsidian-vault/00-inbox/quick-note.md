A quick capture with no frontmatter.

This note intentionally has no YAML header so we can test the fallback
to using the filename as the title.

- TODO: process this later
- It references [[q2-planning]] somewhere
